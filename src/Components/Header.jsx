import React from "react";

const Header = ({ score }) => {
  return (
    <>
      <div>Score: {score}</div>
      <div>---------------------------------</div>
    </>
  );
};

export default Header;
